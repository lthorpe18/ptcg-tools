# CP02 — real deck workspace (not started)

Start only after CP01 review/merge and explicit acceptance to proceed. CP01 does
not authorize CP02 or a production cutover. Read CP01-COMPLETION.md first.

## Complete next-workspace prompt

Continue the PTCG Tools UI/UX rebuild in `lthorpe18/ptcg-tools`.
Execute checkpoint 02 only: the real deck workspace reference implementation.

Before changing anything:

1. Check applicable AGENTS.md, current remote main, the local worktree and open
   PRs. Verify that the reviewed CP01 foundation has been merged and its changes
   are present. If not, report the missing prerequisite; do not merge it or
   silently create a stacked CP02 branch. Preserve newer generated-feed commits.
2. Read CURRENT_STATE.md, docs/UI_UX_REBUILD_STATUS.md,
   docs/UI_UX_REBUILD_GUIDE.md, docs/UI_UX_DECISIONS.md,
   docs/UI_UX_ROUTE_PARITY.md, handoffs/ui-ux/CP-01-COMPLETION.md,
   handoffs/ui-ux/CP-02.md, v3-preview/README.md,
   PERFORMANCE_ARCHITECTURE.md, CARD_IMAGE_ARCHITECTURE.md and
   COMMUNITY_AND_ACCOUNT_ARCHITECTURE.md. Follow their references when the
   existing deck/storage/version contracts require them.
3. Inspect the canonical Deck/DeckVersion/listHash/storage actions, deck editor,
   import/export/validation and existing account-sync ownership. Inspect V3
   routing/history, presentation components and image adapter. Record exact
   existing URLs/actions before designing an adapter; do not guess identities.
4. State the bounded plan and proceed without another approval unless a genuine
   product, authorization or unsafe persistence decision blocks implementation.

Implement under `v3-preview/`, keeping the existing plain HTML/CSS/JavaScript
stack and CP01 visual system. Deliver one real saved-deck workspace showing its
complete list, grouped into Pokémon, Trainers and Energy. Make the list the
primary surface. Use the shallow horizontal ArtworkHeader with only the name
overlaid; no separate title/metadata panel or hero action. Keep useful count,
version and actions in compact controls outside the artwork.

Reuse CP01 list/gallery, rows, filter/disclosure, inspector, error/retry, tokens
and return-context patterns. Preserve established editing, quantities,
validation, save, import/export and undo/error behaviour where supported. Retain
existing secondary actions through explicit V2 transitions until their own
checkpoint. Do not migrate the whole deck index, standalone search, version
browser, results or Playtest workflows now; these belong to later checkpoints.
Use the smallest functional entry to an actual saved deck and preserve its
origin/back state. Handle invalid/deleted deck IDs explicitly.

Consume existing domain services through narrow adapters. No copied engines,
duplicate stores, second account-sync controller, new schema or changed evidence
semantics. Prove the single sync owner and account-switch isolation before live
writes. Viewing must not create a deck/version. Artwork choice must not mutate
Deck ID, exact printing, listHash or immutable DeckVersion identity. Continue to
use `window.PTCGCardImages` and `window.DeckSprites.html()` unchanged.

Do not change production root, V2, current PWA manifest/service-worker scope,
ETL, Meta calculations or generated data. Do not redirect users to V3. Do not
implement Collection or show invented ownership/readiness figures or a dead tab.
Do not redesign auth or introduce a framework.

Verify a real complete 60-card list, long names, missing art, list/gallery,
inspector and dialog focus, save/edit/error/retry and supported undo actions.
Test correct identities and unchanged immutable versions. Use the repository's
supported browser-preview process and inspect actual rendered layouts at 390px,
360px, desktop and enlarged text/200% zoom. Check visible keyboard focus,
reduced motion, overflow, safe-area/bottom-nav spacing, direct reload,
Back/Forward and legacy return with filters/scroll restored. Use safe isolated
test data; do not overwrite the owner's decks. Run focused regression tests for
the touched persistence/domain boundaries, not every unrelated test. Never
weaken existing failures. Record real iPhone acceptance separately if unavailable.

Update the checkpoint ledger, decisions, exact route parity and current state;
create the CP02 completion handoff and prepare the complete CP03 prompt. Record
any remaining CP01 limitations honestly. Create one bounded branch and reviewable
PR. Do not merge without explicit authorization in this chat and do not start
CP03. Finish with separate implementation, tests, browser, preview, PR/SHA,
production deployment, owner-device, baseline/new failures, data/schema,
limitations, rollback and exact-next-action statuses, then the full CP03 prompt.
