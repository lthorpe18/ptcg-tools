(function(){
'use strict';
const STAGES=[['asym-top-16','Asym Top 16'],['asym-top-8','Asym Top 8'],['asym-top-4','Asym Top 4'],['top-16','Top 16'],['top-8','Top 8'],['top-4','Top 4'],['finals','Finals']];
const LABELS=Object.fromEntries(STAGES);
let selected=null,originalPut=null;
const $=id=>document.getElementById(id);
function inject(){const form=$('roundForm');if(!form||$('roundStagePicker'))return;const block=document.createElement('div');block.id='roundStagePicker';block.className='field-block round-stage-picker';block.innerHTML=`<span>Stage <small>optional</small></span><div class="round-stage-buttons"><button type="button" data-stage="">Swiss</button>${STAGES.map(([value,label])=>`<button type="button" data-stage="${value}">${label}</button>`).join('')}</div>`;const more=form.querySelector('.round-more');(more||form.lastElementChild)?.insertAdjacentElement('beforebegin',block);block.querySelectorAll('[data-stage]').forEach(button=>button.addEventListener('click',()=>setStage(button.dataset.stage||null)));const style=document.createElement('style');style.textContent=`.round-stage-picker>span{display:flex;align-items:baseline;gap:5px}.round-stage-picker>span small{color:var(--app-muted-2);font-size:9px}.round-stage-buttons{display:flex;gap:6px;overflow-x:auto;padding:4px 0 2px;scrollbar-width:none}.round-stage-buttons::-webkit-scrollbar{display:none}.round-stage-buttons button{flex:0 0 auto;min-height:34px;border:1px solid var(--app-border);border-radius:999px;background:#fff;color:var(--app-muted);padding:0 10px;font:inherit;font-size:10px;font-weight:800}.round-stage-buttons button.active{border-color:#84adff;background:var(--app-blue-soft);color:var(--app-blue)}.round-stage-chip{display:inline-flex;align-items:center;min-height:18px;padding:0 6px;border-radius:999px;background:#f4f3ff;color:#5925dc;font-size:8px;font-weight:850;white-space:nowrap;margin-left:5px}`;document.head.appendChild(style);setStage(null)}
function setStage(stage){selected=stage||null;document.querySelectorAll('#roundStagePicker [data-stage]').forEach(button=>button.classList.toggle('active',(button.dataset.stage||null)===selected))}
function stageFromMatch(id){const match=id?window.PTCGMatchStore?.get?.(id):null;setStage(match?.roundStage||null)}
function wrapStore(){if(!window.PTCGMatchStore?.put||originalPut)return;originalPut=window.PTCGMatchStore.put.bind(window.PTCGMatchStore);window.PTCGMatchStore.put=input=>originalPut({...input,roundStage:selected||null})}
function decorateHistory(){document.querySelectorAll('#roundHistory [data-match-id]').forEach(row=>{row.querySelector('.round-stage-chip')?.remove();const match=window.PTCGMatchStore?.get?.(row.dataset.matchId);if(!match?.roundStage||!LABELS[match.roundStage])return;const target=row.querySelector('.rich-round-number')||row.firstElementChild;if(!target)return;target.insertAdjacentHTML('afterend',`<span class="round-stage-chip">${LABELS[match.roundStage]}</span>`)})}
function bind(){inject();wrapStore();$('addRound')?.addEventListener('click',()=>setTimeout(()=>setStage(null),0));document.addEventListener('click',event=>{const row=event.target.closest?.('#roundHistory [data-match-id]');if(row)setTimeout(()=>stageFromMatch(row.dataset.matchId),0)});$('deleteRound')?.addEventListener('click',()=>setStage(null));const history=$('roundHistory');if(history)new MutationObserver(()=>setTimeout(decorateHistory,20)).observe(history,{childList:true,subtree:true});window.addEventListener('ptcg:local-change',()=>setTimeout(decorateHistory,40));setTimeout(decorateHistory,120)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
