(function(){
'use strict';
const $=id=>document.getElementById(id);
const now=()=>new Date().toISOString();
const today=()=>new Date().toISOString().slice(0,10);
const uid=prefix=>`${prefix}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
function esc(value){return String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[ch]))}
function attendingRows(){return (window.PTCGStorage?.allParticipations?.()||[]).filter(row=>row&&row.attendanceStatus==='attending'&&!row.completion).sort((a,b)=>String(a.eventSnapshot?.startDate||'').localeCompare(String(b.eventSnapshot?.startDate||'')))}
function label(row){const s=row.eventSnapshot||{};const name=s.name||s.venue||s.city||'Saved event';const date=s.startDate?new Date(`${s.startDate}T12:00:00`).toLocaleDateString('en-GB',{day:'numeric',month:'short'}):'';return date?`${name} · ${date}`:name}
function buildForm(){const form=$('recordTournamentForm');if(!form)return;form.innerHTML=`
  <div class="record-section"><span class="record-label">Attending event <small>optional</small></span><select id="recordEventSelect"><option value="">No linked event</option></select><small id="recordEventHint" class="record-hint"></small></div>
  <div id="manualTournamentFields"><div class="record-divider">or create a tournament</div><div class="record-field record-name"><span class="record-label">Name</span><input id="recordName" type="text" autocomplete="off" placeholder="Friday League / Online Cup"></div><div class="record-quick-row"><div class="record-field"><span class="record-label">Date</span><input id="recordDate" type="date"></div><div class="record-field"><span class="record-label">Type</span><select id="recordType"><option value="League / Local">League / Local</option><option value="League Challenge">Challenge</option><option value="League Cup">Cup</option><option value="Prerelease">Prerelease</option><option value="Regional">Regional</option><option value="International">International</option><option value="World Championships">Worlds</option><option value="Online Tournament">Online</option><option value="Other">Other</option></select></div></div></div>
  <div id="recordError" class="record-error hidden"></div><button id="recordSubmit" class="record-submit" type="submit">Start tournament</button>`}
function openSheet(){renderEvents();$('recordDate').value=$('recordDate').value||today();$('recordBackdrop').classList.remove('hidden');$('recordBackdrop').setAttribute('aria-hidden','false');document.body.style.overflow='hidden';syncMode()}
function closeSheet(){$('recordBackdrop').classList.add('hidden');$('recordBackdrop').setAttribute('aria-hidden','true');document.body.style.overflow=''}
function renderEvents(){const rows=attendingRows();$('recordEventSelect').innerHTML=`<option value="">No linked event</option>${rows.map(row=>`<option value="${esc(row.id)}">${esc(label(row))}</option>`).join('')}`;$('recordEventHint').textContent=rows.length?'Select one, or leave blank to record anything else.':'Record any local or online tournament below.'}
function syncMode(){const linked=!!$('recordEventSelect').value;$('manualTournamentFields').classList.toggle('hidden',linked);$('recordSubmit').textContent=linked?'Start selected event':'Start tournament'}
function createAdHoc(){const name=$('recordName').value.trim();if(!name){$('recordError').textContent='Give the tournament a name.';$('recordError').classList.remove('hidden');return null}const date=$('recordDate').value||today();const type=$('recordType').value||'League / Local';const context=type==='Online Tournament'?'online':'irl';const id=uid('participation');const created=now();window.PTCGStorage.update(state=>{state.eventParticipations=Array.isArray(state.eventParticipations)?state.eventParticipations:[];state.eventParticipations.push({modelVersion:1,id,eventId:null,source:'manual',sourceId:null,attendanceStatus:'attending',phase:'preparation',eventSnapshot:{id:null,officialEventId:null,source:'manual',sourceId:null,scope:context,type,name,venue:context==='online'?'Online':null,startDate:date,startTime:null,endDate:null,endTime:null,address:null,city:null,region:null,postcode:null,country:null,latitude:null,longitude:null,distanceFromSeedMiles:null,cost:null,officialUrl:null,registrationUrl:null,secondarySourceUrl:null,season:null},prep:null,plannedDeckRef:{},usedDeckRef:null,tournamentDay:null,completion:null,seasonId:null,archivedAt:null,createdAt:created,updatedAt:created});return state});return window.PTCGStorage.getParticipation(id)}
function ensureTournamentDayCompatible(row){if(!row)return null;if(row.plannedDeckRef)return row;return window.PTCGStorage.updateParticipation(row.id,current=>{current.plannedDeckRef={};return current})}
function relocateSavedFilter(){const filter=$('savedVenuesFilter'),controls=$('eventControls');if(!filter||!controls||filter.classList.contains('saved-venues-standalone'))return;filter.classList.add('saved-venues-standalone');controls.insertAdjacentElement('afterend',filter)}
function submit(event){event.preventDefault();$('recordError').classList.add('hidden');const linkedId=$('recordEventSelect').value;let row=linkedId?window.PTCGStorage.getParticipation(linkedId):createAdHoc();if(!row)return;row=ensureTournamentDayCompatible(row);closeSheet();location.href=`./tournament-day.html?participation=${encodeURIComponent(row.id)}`}
function bind(){const button=$('recordTournamentButton');if(!button)return;relocateSavedFilter();buildForm();button.addEventListener('click',openSheet);$('closeRecordSheet').addEventListener('click',closeSheet);$('recordBackdrop').addEventListener('click',event=>{if(event.target===$('recordBackdrop'))closeSheet()});$('recordEventSelect').addEventListener('change',syncMode);$('recordTournamentForm').addEventListener('submit',submit)}
bind();
})();